package lycan.util;

class Ngrams {
    // TODO ideally implement iterator for iterating over strings that stops at each n-th delimiter
    // TODO or because it's easier to write, methods to create arrays of ngrams from the strings e.g. sets of digrams like "doug said" -> "do" "ou" "ug" "sa" "ai" "id"
}